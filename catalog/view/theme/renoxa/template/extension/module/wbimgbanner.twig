
<div class="container">
	<div class="row">
<div class=" imgbnr col-lg-4 col-md-6 col-sm-0 col-xs-12">
    {% for banner in banners %}
    <div class="sub-img">
    	<div class="beffect">
	        {% if banner.link %}
			    <a href="{{ banner.link }}">
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
			    </a>
			    {% else %}
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
		    {% endif %}
		</div>
	</div>
    {% endfor %}
</div>
<div class="cat-tab col-lg-8 col-md-6 col-sm-12 col-xs-12">

